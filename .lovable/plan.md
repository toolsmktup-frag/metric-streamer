

Vou gerar um arquivo SQL único com tudo que precisa rodar no Supabase pra ativar o sistema de tags.

### Conteúdo do arquivo

Mesmo SQL de `docs/sql/lead_tags.sql` (tabelas `lead_tags`, `lead_tag_assignments`, RLS, RPC `get_org_tags_with_usage`), salvo em `/mnt/documents/setup-tags.sql` pra você baixar e colar no SQL Editor do Supabase.

### Resultado

Um `.sql` único, pronto pra copiar/rodar.

