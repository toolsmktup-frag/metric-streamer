# Correções 2026-07-22 — Carrinho abandonado ponta a ponta (Ticto + Guru) + auditoria da entrega

Contexto: a Gabi reportou "não entra carrinho abandonado" (suplemento e info). A investigação
puxou 5 fios encadeados — de bugs nossos a um problema fora do sistema. Tudo em produção; o PR
#87 registra as correções de código no repo.

Memória de sessão relacionada: `carrinho-abandonado-fix-completo`.

---

## 1. Trigger com filtro de produto vazio nunca disparava _(wz-receiver, PR #87)_

`productIdFilter`/`offerFilter` com array vazio `[]` é **truthy** em JS → o código entrava no
`if` e comparava o produto contra uma lista vazia → **nenhum evento casava**. O flow "Guia das
Tinturas" ficou ~60 dias sem uma única execução. Fix: `[]` agora significa "sem filtro"
([wz-receiver/index.ts](../supabase/functions/wz-receiver/index.ts), `matchesTrigger`).

## 2. Envio sem sucesso logava "success" (falha silenciosa) _(wz-executor, PR #87)_

Nó WhatsApp que não enviou nenhum bloco e teve erro (ex.: `HTTP 503` de instância desconectada)
marcava o log como `success` com "0 enviados" — 12+ dias de falha invisível (instância
"Automação 1" caiu em 21/04). Agora, nada enviado + erro = log `failed` com a mensagem. Flows
re-apontados da "Automação 1" para **SoulNaturi** (`554891575679`).

## 3. Guru descartava carrinho abandonado antes de criar o card _(guru-webhook, PR #87)_

A Guru **manda** abandono (`status: "abandoned"` — confirmado por evento real 22/07 10:15), mas o
`statusMap` do `guru-webhook` não o reconhecia → caía em "unknown status, skipping" **antes** do
`sync_lead_from_sale`. Resultado: a automação até disparava (o forward pro `wz-receiver` acontece
antes do descarte), mas **nenhum card nascia no kanban** — exatamente o que a Gabi via.

Fix:
- `statusMap` traduz `abandoned`/`abandoned_cart`/`cart_abandoned`/`checkout_abandoned` → `abandoned_cart`.
- Abandono **não** entra em `customer_purchases` (o upsert usa `onConflict` platform+transaction_id,
  e abandono não tem transaction_id → cada um sobrescreveria o anterior). Só audita + sincroniza o
  lead; as `stage_transition_rules` movem o card (**RECOMPRA-POTES → "Recuperar"**).

## 4. Cegueira total da Guru na auditoria _(guru-webhook, PR #87)_

`webhook_audit` era **só da Ticto** — `guru-webhook` e `eduzz-webhook` não escreviam nada. Isso
atrasou o diagnóstico em ~1 dia (a Guru parecia "não mandar", quando na verdade a gente não
registrava). Agora o `guru-webhook` audita com `source='guru'`, inclusive nos descartes de status
desconhecido e nas rejeições 401. **Eduzz continua sem auditar** (pendência).

## 5. Lookup de lead por telefone falhava (vendedora não atribuída) _(wz-executor, PR #87)_

`.eq("phone", "5571999853141")` não achava o mesmo lead salvo como `"+5571999853141"` ou
`"71999853141"` → "Lead não encontrado pelo telefone", sem atribuir vendedora nem aplicar tag.
Novo helper `findLeadByContact` gera as formas BR (DDI 55, 9º dígito, prefixo `+`) e busca por
`.in()`, com fallback por e-mail — aplicado nos 4 nós (variáveis/tags, ab_split, tag, mover funil).

## 6. Copys dos flows (feito direto no banco, sem PR)

- **Guia das Tinturas**: era clone do Articulabem falando "pote grátis/frete/transportadora" para
  um **e-book**. Reescrito (4 nós × 3 variações). Compra aprovada agora entrega os acessos:
  portal `portal.cursosmatheuscolombo.com.br`, login = `{{email}}` do comprador, senha padrão
  `123456` (igual para todos), + tutorial e nota do bump Curso Mestre.
- **RevitaSoul**: removido o framing de articulações herdado do Articulabem (é outro pote, sem
  claim documentado). Filtros de trigger apontados para os IDs Guru reais.
- Filtros do Guia: `purchase_approved`/`pix_generated`/`payment_refused` → `["46342","47629"]`;
  `cart_abandoned` → `["46342"]`.

---

## Validação (eventos reais, não sintéticos)

- Teste de abandono real disparado no checkout da Ticto (Guia, `OA0FC6C5C`): registrado 14:45 →
  **entregue 14:50** → card em Infoprodutos → Carrinho Abandonado → automação disparada. Repetido
  15:10 → entregue 15:15. **A entrega Ticto→nós funciona.**
- Abandono Guru sintético (Articulabem): audit ✓, card em **RECOMPRA-POTES → "Recuperar"** ✓,
  execução do flow ✓, **vendedora atribuída** via `findLeadByContact` ✓.
- Abandono Guru real (Josete, 10:15): chegou, processou, mensagem saiu pela SoulNaturi.
- Todos os dados de teste foram removidos.

## 7. O evento que NÃO era nosso — parada de tráfego ~09:50

Durante a validação: a **Ticto parou de entregar QUALQUER evento às 09:50:03** e a **Guru às
10:15** (últimos eventos reais). Diagnóstico por eliminação:
- Nosso endpoint responde 200 em <1s (testado ao vivo) e audita **toda** requisição que chega —
  zero linhas após 09:50 = zero requisições chegando, não requisições falhando.
- `ticto-webhook` sem deploy desde 01/07; os deploys de hoje (guru/wz-executor) foram ~13:40,
  **depois** do silêncio.
- **A entrega do Guia foi provada funcionando 5h depois** (testes 14:50/15:15) → o cano está
  aberto; o que faltou foi evento, ou seja, **ninguém entrando no checkout**.

Conclusão: **não é CRM, não é Ticto, não é Guru** — as duas plataformas pararam juntas na janela
09:50–10:15 porque a fonte de visitantes (tráfego pago do Meta) aparentemente parou. **Pendente de
confirmação pelo Matheus:** campanhas do Guia/Articulabem ativas e gastando após 10h? Há venda
registrada (Ticto/Guru) após 10h? Se houver venda sem evento chegando → volta a ser investigação
nossa; se não houver venda → confirma tráfego parado (conserto no Meta, não no código).

---

## Pendências

- **Eduzz**: sem auditoria e sem tratamento de abandono (mesmo furo que a Guru tinha).
- **Checkout Ticto**: pede telefone tarde no formulário → ~52% dos abandonos do Guia chegam sem
  telefone (irrecuperáveis por WhatsApp). Ajuste no painel da Ticto, com o Matheus.
- **Migração dos infoprodutos** da SoulNaturi para a Girassol (decisão da Sarini: SoulNaturi = só
  físicos). O Guia ainda está na SoulNaturi.
- **Flow "Poder Holístico"**: aponta para instância `lan-holistico` desconectada.
- **Flow "Webinário — comprou o Curso dos Erveiros"**: `purchase_approved` **sem filtro de
  produto** → dispara para qualquer compra.
- **Webhook "ad metricis -"** (token `1f6e...`) no painel da Guru não existe do nosso lado —
  entrada morta, limpar.
