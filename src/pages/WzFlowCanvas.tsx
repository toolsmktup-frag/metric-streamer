import React from 'react';

// Placeholder — será implementado na Fase 2 (canvas React Flow)
export default function WzFlowCanvas() {
  return (
    <div className="flex items-center justify-center h-[calc(100vh-120px)]">
      <div className="text-center">
        <h2 className="text-xl font-semibold text-foreground mb-2">Canvas de Fluxo</h2>
        <p className="text-muted-foreground">Em construção — Fase 2</p>
      </div>
    </div>
  );
}
