

## Plano: Gerar documento com as 3 Edge Functions atualizadas

Vou gerar um arquivo de texto com o código completo das 3 edge functions (ticto-webhook, guru-webhook, eduzz-webhook) já com `checkout_url` e `page_url` incluídos, para você copiar e colar no Supabase Dashboard.

### Etapa única
- Criar um arquivo `.txt` em `/mnt/documents/` com as 3 funções separadas por cabeçalhos claros
- Cada seção terá o nome da função e o código completo do `index.ts`

### Detalhes Técnicos
Os códigos já estão atualizados no repositório com a extração de `checkout_url` e `page_url`. Vou apenas consolidar os 3 arquivos num documento único para facilitar o copy-paste.

